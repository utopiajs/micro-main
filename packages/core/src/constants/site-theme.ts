import { theme, type ThemeConfig } from 'antd';

const siteThemeConfig: ThemeConfig = {
  algorithm: theme.defaultAlgorithm,
  token: {
    colorPrimary: '#1677FF',
    borderRadius: 4
  }
};

export { siteThemeConfig };
