{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "dev:core": "lerna run dev --scope=@utopia/micro-main-core"
  },
  "devDependencies": {
    "lerna": "^6.0.3"
  }
}
